import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.winchurchfamily.guessthesandwich',
  appName: 'Guess the Sandwich',
  webDir: 'dist'
};

export default config;
